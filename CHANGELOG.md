## 0.1.0

- First release: error capture (Flutter + zone), breadcrumbs, product events, user identification, queueing and retry, frame-based session replay (`SightpaneReplay`, `SightpaneMask`), tap and navigation breadcrumbs, `SightpaneHttpClient`.
